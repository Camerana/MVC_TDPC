@{
    ViewData["Title"] = "Delete Person";
}
@using System.Collections.Generic;
@model List<PersonModel>;
<h1>@ViewData["Title"]</h1>

<div class="container">
    <div class="row">
        <div class="col-12">
            <h3 class="display-4">Test Javascript Post Person</h3>
            ID: <input type="text" id="personID" />
            <button class="btn btn-primary" onclick="deletePerson()">Delete Person</button>
        </div>
    </div>
    @{
        <table>
            @for (int i = 0; i < Model.Count; i++)
            {
                <tr>
                    <td>@Model[i].Nome</td>
                    <td>@Model[i].Cognome</td>
                    <td><button class="btn-sm btn-danger" onclick="deletePersonById('@Model[i].ID')">Delete</button></td>
                </tr>
            }
        </table>
    }
</div>